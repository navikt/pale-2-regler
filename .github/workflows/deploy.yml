name: Deploy app to dev and prod
on:
  push:
  workflow_dispatch:
permissions:
  actions: read
  contents: write
  security-events: write
  packages: write
  id-token: write

jobs:
  jar-app:
    uses: navikt/teamsykmelding-github-actions-workflows/.github/workflows/plain-jar-app-25.yaml@main
    secrets: inherit
    with:
      app: pale-2-regler
